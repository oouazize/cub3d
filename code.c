#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
#include <math.h>

int main()
{
    int len;
    int j = -1;
    scanf("%d", &len);
    while (++j < len)
    {
    return (0);
}